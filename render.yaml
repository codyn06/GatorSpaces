services:
  - type: web
    name: gatorspaces
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps chromium
    startCommand: gunicorn tester:app
